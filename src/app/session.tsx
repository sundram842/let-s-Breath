import { BreathingScreen } from '@/features/breathing';

// The breathing exercise, pushed over the tabs from Home's Start / Resume button.
export default function Session() {
  return <BreathingScreen />;
}
